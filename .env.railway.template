# Railway Environment Variables Template
# Copy these to your Railway project's Variables section

# These will be automatically set by Railway when you add PostgreSQL:
# DATABASE_URL=<automatically set by Railway>
# PGUSER=<automatically set by Railway>
# PGPASSWORD=<automatically set by Railway>

# You need to manually add these:
DB_DRIVER=org.postgresql.Driver
DB_DIALECT=org.hibernate.dialect.PostgreSQLDialect
DDL_AUTO=update
SHOW_SQL=false
H2_CONSOLE_ENABLED=false

# Optional: Custom security credentials
# SPRING_SECURITY_USER_NAME=admin
# SPRING_SECURITY_USER_PASSWORD=your-secure-password-here
